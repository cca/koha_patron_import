[project]
name = "patron-import"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "click>=8.1",
    "docopt>=0.6",
    "email-validator>=2.2",
    "pydantic>=2.11",
    "requests==2.32.5",
    "termcolor>=3.1",
]

[dependency-groups]
dev = [
    "pytest>=8.4",
]
